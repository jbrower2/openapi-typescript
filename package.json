{
	"name": "@jbrower/openapi-typescript",
	"version": "1.0.2",
	"description": "Tool for generating TypeScript APIs/clients from OpenAPI spec files.",
	"main": "dist/index.js",
	"bin": "cli.js",
	"scripts": {
		"build": "tsc",
		"prestart": "npm run build",
		"start": "node .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Jeff Brower",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "github:jbrower2/openapi-typescript"
	},
	"dependencies": {
		"email-validator": "^2.0.4",
		"rimraf": "^3.0.2",
		"yaml": "^1.10.0"
	},
	"devDependencies": {
		"@types/node": "^14.14.31",
		"@types/rimraf": "^3.0.0",
		"typescript": "^4.2.3"
	},
	"peerDependencies": {
		"express": "^4.17.1"
	}
}
